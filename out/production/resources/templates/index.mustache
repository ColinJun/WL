{{>layout/header}}

<h1>스프링 부트로 시작하는 웹 서비스</h1>
<input type="text" name="searchVal" id="searchVal" title="소환사명을 입력하세요." placeholder="소환사명을 입력하세요." onkeydown="if(window.event.keyCode==13){main.search(); return false;}">


{{>layout/footer}}


<script>
var main = {
    init : function () {
        var _this = this;
    },
    search : function () {
        if( $('#searchVal').val() == ""){
            alert("소환사명을 입력하세요.");
        } else {
            window.location.href = '/summoner/'+$('#searchVal').val();
        }
    }
};

main.init();
</script>
<!-- 1. DTO 사용법  - API는 network상 get이다 어떻게 rest api를 쓰는건가?
get도 rest api인가? 요청은 get이되
받는 건 post인가?
2. Input box 통신 방법 알아내기..
3. 서비스 로직 어디다 구현 알지 알아내기


1. 웹앱 구현하는 법
2. API불러오는로직을 어디다쓸지고민하기

흠.. 일단은 ..
get으로 던진다.
근데 포스트방식은 언제 쓰지?

그냥 jsp, mustache로 하면..
REST API 데이터와 view를 같이 던져줘야하려나 - 아니다 RestController 붙이면 view안넘어간다.
사람들이 SPA(vue, react)쓰라고하는데... 흠

조졸두 - 페이지이동 + 모델데이터 + 컨트롤러 = ViewResolver + Model + Controller 페이지이동이 주가 되면서 데이터를 읽을 때.
ajax + RESTAPI + done시 location.href = Location.href + AJAX + RESTAPICONTROLLER 데이터접근이 주가되면서 완료되고 나면 페이지를 이동할 때

1. 조졸두방식으로 만든다.(클리어)
2. 최근 전적 불러오는 API 딴다.
2. REST API는 rest controller를 안써도되는건가?
3. 검색창 이쁜 ui찾는다.
4. 검색하면 실시간 반영되는(ajax도 된다 현재 kdb가 그렇게 쓰는중)
5. spa 사용한다.(상) 이거하면 JSON통신만해도된다는데?
-->

